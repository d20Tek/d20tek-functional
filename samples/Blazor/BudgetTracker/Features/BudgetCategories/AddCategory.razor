@page "/budget-category/add"
@inject ICategoryRepository _repo;
@inject NavigationManager _nav;

<PageTitle>Budget Tracker - Categories</PageTitle>
<div class="col-12 border border-secondary p-1">
    <h3>New Budget Category</h3>

    <EditForm class="mx-2" Model="@_vm" OnValidSubmit="CreateCategory">
        <div class="mb-3">
            <label for="accountName" class="form-label">Name</label>
            <InputText id="accountName" class="form-control" required @bind-Value="_vm.Name" placeholder="New budget category" />
        </div>

        <div class="mb-3">
            <label for="accountAmount" class="form-label">Amount</label>
            <InputNumber id="accountAmount" class="form-control" min=0 @bind-Value="_vm.BudgetedAmount" />
        </div>

        <button type="submit" class="btn btn-primary">Create</button>
        <button type="button" class="btn btn-secondary" @onclick=CancelHandler>Cancel</button>
    </EditForm>

    @if (string.IsNullOrEmpty(_errorMessage) is false)
    {
        <div>@_errorMessage</div>
    }
</div>
